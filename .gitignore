# Real data lives in the private companion repo, never here.
data.json
history.json

# Local-only notes / scratch
*.local
*.local.md
.DS_Store

# Claude Code local settings (contains local machine paths — never commit to this public repo)
.claude/settings.local.json
